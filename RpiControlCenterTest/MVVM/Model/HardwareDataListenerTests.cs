using HardwareMonitor.Models;

namespace HardwareMonitorTests.UnitTests.Model
{
    public class HardwareDataListenerTests
    {
        [TestMethod]
        public void TestHardwareDataListener()
        {
    
            //// Arrange
            //var listener = new HardwareDataListener();
            //var dataReceived = false;
            //// Act
            //listener.HardwareDataReceived += (sender, args) =>
            //{
            //    dataReceived = true;
            //    Assert.IsNotNull(args.Data);
            //    Assert.IsTrue(args.Data.Length > 0);
            //};
            //listener.StartListening();
            //// Wait for some time to allow data to be received
            //Task.Delay(5000).Wait();
            //// Assert
            //Assert.IsTrue(dataReceived, "No hardware data was received.");
        }
    }
}
